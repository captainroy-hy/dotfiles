" iamcco/markdown-preview.nvim

" run `npm install` in '~/.local/share/nvim/plugged/markdown-preview.nvim/app'
" if error occurs when run MarkdownPreview

let g:mkdp_auto_close=0
let g:mkdp_refresh_slow=1
let g:mkdp_markdown_css=$VIM_CONFIG_DIR . '/github-markdown.css'

